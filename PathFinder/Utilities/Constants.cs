using System;

namespace Aptacode.PathFinder.Utilities {
    public static class Constants
    {
        public static readonly float Tolerance = 0.001f;
        public static readonly float Root2 = (float) Math.Sqrt(2.0);
    }
}